{
  "name": "hospital-system-root",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=hospital_system"
  },
  "engines": {
    "node": "22.x"
  }
}
